tab разворачивает

!tab базовая html разметка
тоже самое html:5

div + tab = ткрывающий и закрывающий а курсор между

btn = button

через : у всех со сылками заполняется

элементы с внутренними элементами напрмер таблицы и ul. в конце +
table+  ul+ ol+

точка для класса как в css
div.red

id через #

для div  сокращенный синтаксис .class

просто точка равно <div className=""></div>
или развернет внутренний тег если внутри по смыслу

два класса подряд через точку class1.class2

сразу id и class = #.class1

атрибуты в квадратный скобках []

вложенность элементов >

через плюс + добавить на томже уровне

подняться на одну ступень в дереве ^ на две ^^ и так далее

если заканчивается на плюс и начинаетс следующий тоже с плюсом то через ()
table++h1

доллар $ задает нумерацию а собака @ может задать с какого числа
через минус в обратном порядке div.class$@5*8  div.class$@-5*8

содержимое через {} а коментарий c{}

lorem*1000  генирируем случайный текст